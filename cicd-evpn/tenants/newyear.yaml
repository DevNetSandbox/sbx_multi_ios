NewYear:
  - name: NewYear
    fabric: fabric1
    segments:
    # can have multiple segments
      - name: production
        network: 192.168.231.0/24
        suppress-arp: true
        connection:
          - iface: 1/15
            leaf: leaf1
            # mode: access/trunk (default is trunk)
          - iface: 1/15
            leaf: leaf2
            # mode: access/trunk (default is trunk)
